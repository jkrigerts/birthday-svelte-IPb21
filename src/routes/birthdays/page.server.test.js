import { describe, it, expect } from 'vitest';
import { load } from './+page.server.js';

describe('/birthdays load function', () => {
	it('returns list of birthdays', () => {
		// TODO next week
	});
});
